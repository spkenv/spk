
pkg: spm/0.1.0

build:
  script:
    - /spfs/bin
      python -m nuitka
        --follow-imports
        --standalone
        --output-dir=build
        --o spm
        --show-progress
        --show-scons
        --show-modules
        spm
