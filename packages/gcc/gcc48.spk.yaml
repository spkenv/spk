pkg: gcc/6.3.1

build:
  options:
  - var: arch
  - var: os
  - pkg: stdfs
  script:
  - mkdir -p $PREFIX/etc/spfs/startup.d/
  - cp activate-gcc-48.{sh,csh} $PREFIX/etc/spfs/startup.d/
install:
  requirements:
  - pkg: stdfs
