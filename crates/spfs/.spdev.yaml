version: 0.20.10
release_notes: |
  * Update tag stream removal to remove all empty parent directories
    * this ensures that ls-tags never shows directories with no tags under them

toolchain:
  - kind: Pipenv

components:
  - name: python
    kind: PythonPackage
    lint:
      python3: false
      black: true
      mypy: true
    test:
      command: pytest
    install:
      enabled: true
    deploy:
      rez:
        enabled: false

  - name: rpm
    kind: RPMPackage
    spec_file: spfs.spec

  - kind: HugoDocs
    name: docs
    location: docs

  - kind: SentryRelease
    name: sentry
    project: spfs
    deploy.enabled: true
