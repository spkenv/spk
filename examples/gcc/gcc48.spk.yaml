pkg: gcc/4.8.5

opts:
- var: arch
- var: os

build:
  script:
  - rpm -ql gcc-4.8.5 | sed 's|^/usr||' > files_to_copy.txt
  - rsync -Rdav --files-from=files_to_copy.txt /usr /spfs
  - rm files_to_copy.txt
