
pkg: spm/0.1.0

build:
  script:
    - python -m nuitka
        --follow-imports
        --output-dir=build
        --o /spfs/opt
        --show-progress
        --show-scons
        --show-modules
        spm
    - ln -s /spfs/opt/spm.dist/spm /spfs/bin/spm
